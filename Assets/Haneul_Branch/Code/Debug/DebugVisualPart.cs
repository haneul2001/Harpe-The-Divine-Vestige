using UnityEngine;

// "이 오브젝트는 디버그 표시의 일부"라는 표식.
//
// 로직은 없다. 캐릭터 연출(피격 깜빡임, 은신 반투명)이 자식 렌더러를 훑을 때
// 이걸 보고 건너뛰기 위한 용도다.
// DebugVisual.MakeRenderer가 자동으로 붙이므로 손으로 달 일은 없다.
public class DebugVisualPart : MonoBehaviour
{
}
